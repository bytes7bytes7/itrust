name: itrust
description: ITrust social network.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  collection: ^1.16.0
  connectivity_plus: ^3.0.2
  cupertino_icons: ^1.0.5
  drift: ^2.4.1
  equatable: ^2.0.5
  flutter_hooks: ^0.18.5+1
  flutter_mobx: ^2.0.6+5
  get_it: ^7.2.0
  injectable: ^2.1.0
  intl: ^0.17.0
  logging: ^1.1.0
  mobx: ^2.1.3
  provider: ^6.0.5
  transparent_image: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.3.3
  flutter_lints: ^2.0.1
  injectable_generator: ^2.1.3
  mobx_codegen: ^2.1.1

flutter:
  generate: true
  uses-material-design: true
